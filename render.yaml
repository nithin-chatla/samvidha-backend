services:
  - type: web
    name: samvidha-api
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn samvidha_api:app
    autoDeploy: true
